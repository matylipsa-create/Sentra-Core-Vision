{
  "name": "sentra-core-server",
  "version": "1.0.0",
  "description": "Sentra Core API Server — WebSocket + REST backend for the Sentra Core motor",
  "type": "module",
  "scripts": {
    "start": "node --import tsx server/index.ts",
    "dev": "node --import tsx --watch server/index.ts"
  },
  "dependencies": {
    "ws": "^8.16.0",
    "idb": "^8.0.0",
    "mqtt": "^5.10.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "tsx": "^4.7.0"
  }
}
